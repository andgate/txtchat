resolver: lts-9.8
packages:
- '.'
extra-deps:

#nix:
#  enable: true
#  packages: [binutils, coreutils, gcc, gmp, zlibStatic]

system-ghc: false

docker:
  enable: true
  repo: "fpco/stack-build:lts-9.8"

image:
  container:
    name: txtchat
#    base: alpine-stack:latest
#    entrypoints:
#    - txtchatll